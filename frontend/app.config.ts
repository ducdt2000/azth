export default defineAppConfig({
  ui: {
    primary: "blue",
    gray: "slate",
    tooltip: {
      default: {
        openDelay: 500,
      },
    },
  },

  app: {
    name: "AZTH",
    description: "Multi-Tenant SSO & OIDC Server",
    theme: {
      primaryColor: "#3b82f6",
      darkMode: true,
    },
  },
});
